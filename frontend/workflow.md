# 基本元素：
节点、连线
# 要求：
有按钮可以新增节点（多样式）
新增的节点会被置于自适应的位置
节点有前后两个端口，用于连接其他节点。
点击节点选中该节点，选中后可以拖动节点到新位置。
选中之后的节点右上角有一个圆形的删除按钮，点击可以删除该节点。
双击节点可以编辑节点内容。
点击节点端口，拖动连线连接到其他节点的端口上。
连线如果没有拖动到另一个节点的端口上，则不会连接并被清除
拖动带有连线的节点时，连线会跟随节点移动
# 技术栈
前端：vue3，Vue Flow框架，element-plus，axios，vite

